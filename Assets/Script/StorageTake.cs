using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StorageTake : MonoBehaviour
{
    // Start is called before the first frame update
    private int _storageSize = 5;
   
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
